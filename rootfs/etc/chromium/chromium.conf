# Default settings for chromium. This file is sourced by /bin/sh from
# the chromium launcher.

# Options to pass to chromium.
#CHROMIUM_FLAGS=""
CHROMIUM_FLAGS="--no-sandbox --no-first-run --disable-gpu"